import { ComingSoon } from "@/components/ui/ComingSoon";

export default function ReportsTab() {
  return (
    <ComingSoon
      icon="assessment"
      title="Reports & history"
      description="Security health score, exportable PDF summaries, and scan history to prove fixes over time."
    />
  );
}
