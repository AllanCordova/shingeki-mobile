import { ComingSoon } from "@/components/ui/ComingSoon";

export default function ScansTab() {
  return (
    <ComingSoon
      icon="security"
      title="Dynamic scans (DAST)"
      description="Run safe, controlled tests against a target URL and review failures with clear impact — without harming production."
    />
  );
}
