import { ComingSoon } from "@/components/ui/ComingSoon";

export default function ProjectsTab() {
  return (
    <ComingSoon
      icon="folder-special"
      title="Projects & workspaces"
      description="Separate analyses per application, wire webhooks for CI, and keep environments organized."
    />
  );
}
