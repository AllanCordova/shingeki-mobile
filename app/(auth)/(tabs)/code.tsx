import { ComingSoon } from "@/components/ui/ComingSoon";

export default function CodeTab() {
  return (
    <ComingSoon
      icon="code"
      title="Source & SAST"
      description="Connect your repository to surface architecture-level security issues and keep static analysis in sync with every commit."
    />
  );
}
